extends layout

block content
  .main.container
    .row.text-xs-center
      .col-md-6.col-md-offset-3.p-t-2
        i.icn-person.material-icons error
        p.lead.m-t-2 That email is already taken.

        div(class='btn-group')
          a(class='btn btn-danger disabled')
            i(class='fa fa-arrow-left' style='width:16px; height:24px')
          a(class='btn btn-danger ' value='Go back.' onclick='history.back(-1)' style='width:12em;') Go back.